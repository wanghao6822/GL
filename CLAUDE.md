# CLAUDE.md — Modbus-IO 远程IO 项目,直流屏 硅链调压器

## 项目概述

基于 STM32F103C8 的 Modbus 远程 IO 设备，使用 PlatformIO 开发，Arduino 框架。

## 技术栈

- **MCU**: STM32F103C8 (Cortex-M3)
- **框架**: Arduino + FreeRTOS
- **通信协议**: Modbus RTU (RS485) / Modbus TCP (Ethernet)
- **构建工具**: PlatformIO
- **语言**: C++

## 关键库

- Modbus-Arduino / Modbus-Serial / Modbus-Ethernet
- ADS1X15 (模拟量采集)
- PCF8575 (I2C 扩展 IO，预留)
- STM32duino FreeRTOS

## 代码风格

- C++ 嵌入式风格，注重资源效率
- 使用 FreeRTOS 任务管理
- 硬件相关代码注意中断安全和看门狗喂狗

## 我的角色

我是嵌入式开发初学者（0基础），需要你帮助进行代码修改、调试和功能添加。
请用通俗易懂的语言解释技术问题，并给出具体的代码修改方案。

## 项目目标

在现有 Modbus-IO 项目基础上，增加直流屏 硅链调压器 控制功能，实现：

io板子采集两路模拟量，模拟量由（合母HM、控母KM）直流电压经电阻分压（3×300kΩ+10kΩ，分压比91:1）+ GS8331运放跟随后通过Modbus上报

io板子用3个继电器（Y2/Y3/Y4）控制硅堆实现8级降压（0~35V），自动维持控母电压等于设定目标值

支持通过Modbus设置目标电压、每档压降、死区等参数，手动/自动模式切换

合母与控母压差异常时进行降压

所有参数支持掉电保存（EEPROM）

完全兼容原有PLC的Modbus寄存器映射（地址0~19）

## IO 引脚总表

### 数字输入（8路，低电平有效，已在 X_filter 任务中做消抖滤波）

| 名称 | 引脚 | 名称 | 引脚 |
| ---- | ---- | ---- | ---- |
| X0   | PB12 | X4   | PA8  |
| X1   | PB13 | X5   | PB0  |
| X2   | PB14 | X6   | PA11 |
| X3   | PB15 | X7   | PA12 |

### 数字输出（6路，开漏模式，LOW=导通/开启，HIGH=断开/关闭）

| 名称 | 引脚 | 当前用途                |
| ---- | ---- | ----------------------- |
| Y0   | PA15 | 报警输出（AlarmOutPin） |
| Y1   | PB3  | 通用输出                |
| Y2   | PB4  | 硅链控制继电器1         |
| Y3   | PB5  | 硅链控制继电器2         |
| Y4   | PB6  | 硅链控制继电器3         |
| Y5   | PB7  | 通用输出                |

### 拨码开关（5路，上拉输入，低电平=ON）

| 名称  | 引脚 | 功能        |
| ----- | ---- | ----------- |
| SW_B1 | PC15 | 站号 bit0   |
| SW_B2 | PA0  | 站号 bit1   |
| SW_B3 | PA1  | 站号 bit2   |
| SW_B4 | PA2  | 波特率 bit0 |
| SW_B5 | PA3  | 波特率 bit1 |

### 指示灯（开漏，低电平点亮）

| 名称      | 引脚 | 说明             |
| --------- | ---- | ---------------- |
| RUN_LED   | PC14 | 每秒翻转         |
| ERROR_LED | PC13 | 看门狗复位后闪烁 |

### 通信与 I2C

| 名称     | 引脚 | 说明                                 |
| -------- | ---- | ------------------------------------ |
| RS485 TX | PB10 | Modbus RTU 发送                      |
| RS485 RX | PB11 | Modbus RTU 接收                      |
| RS485 EN | PB1  | RS485 发送使能                       |
| I2C SDA  | PB9  | ADS1115 + PCF8575                    |
| I2C SCL  | PB8  | ADS1115 + PCF8575                    |
| 调试 TX  | PA9  | 串口打印（需开启 UseSerialPrint 宏） |
| 调试 RX  | PA10 | 串口打印                             |

### 模拟量输入（ADS1115，I2C 地址 0x48，增益 ±4.096V）

| 名称 | ADS1115 通道 | Modbus 寄存器 | 用途                            |
| ---- | ------------ | ------------- | ------------------------------- |
| AI0  | 通道 0       | 寄存器 15     | 合母电压（HM，经降压模块 99:1） |
| AI1  | 通道 1       | 寄存器 16     | 控母电压（KM，经降压模块 99:1） |
| AI2  | 通道 2       | 寄存器 17     | 预留                            |
| AI3  | 通道 3       | 寄存器 18     | 预留                            |

## Modbus 保持寄存器完整映射（地址 0~29，RTU/TCP 共享）

### 基础 IO 寄存器（0~19，保持兼容）

| 地址 | 名称          | 读写 | 说明                                                                  |
| ---- | ------------- | ---- | --------------------------------------------------------------------- |
| 0    | 固件版本      | R    | 固定值 40501                                                          |
| 1    | 从站 ID       | R    | 拨码开关 SW_B1~B3 决定                                                |
| 2    | 波特率状态    | R    | 0=115200, 1=9600, 2=19200, 3=38400                                    |
| 3    | 参数操作      | R/W  | 写 10=保存, 20=重载, 30=重启, 66=恢复出厂                             |
| 4    | 滤波时间      | R/W  | 默认 5ms，范围 1-100                                                  |
| 5    | MAC 低 2 字节 | R/W  | MAC[0] + MAC[1]                                                       |
| 6    | MAC 中 2 字节 | R/W  | MAC[2] + MAC[3]                                                       |
| 7    | MAC 高 2 字节 | R/W  | MAC[4] + MAC[5]                                                       |
| 8    | IP 低 2 字节  | R/W  | IP[2].IP[3]                                                           |
| 9    | IP 高 2 字节  | R/W  | IP[0].IP[1]                                                           |
| 10   | 运行时间      | R    | 设备运行秒数（0-65535 循环）                                          |
| 11   | 输入状态      | R    | bit0=X0 ... bit7=X7                                                   |
| 12   | 输出状态      | R/W  | bit0=Y0, bit1=Y1 ... bit5=Y5（自动模式下 Y0/Y2/Y3/Y4 由硅链逻辑接管） |
| 13   | 扩展输入      | R    | 预留（PCF8575 扩展 IO）                                               |
| 14   | 扩展输出      | R/W  | 预留（PCF8575 扩展 IO）                                               |
| 15   | AI0 原始值    | R    | 合母电压 ADC 值（经91:1分压+GS8331跟随）                               |
| 16   | AI1 原始值    | R    | 控母电压 ADC 值（经91:1分压+GS8331跟随）                               |
| 17   | AI2 原始值    | R    | 预留模拟量                                                            |
| 18   | AI3 原始值    | R    | 预留模拟量                                                            |
| 19   | 保留/测试     | R/W  | 看门狗测试用                                                          |

### 硅链调压控制寄存器（20~28，新增）

| 地址 | 名称        | 读写 | 默认值 | 说明                                                                     |
| ---- | ----------- | ---- | ------ | ------------------------------------------------------------------------ |
| 20   | HM 实际电压 | R    | —     | 合母电压 ×100（24300=243.00V），由 ADC 原始值 × 分压系数(9100) 换算     |
| 21   | KM 实际电压 | R    | —     | 控母电压 ×100，由 ADC 原始值 × 分压系数(9100) 换算                     |
| 22   | 当前档位    | R    | —     | 0~7，当前硅链降压档位                                                    |
| 23   | 报警状态    | R    | —     | 0=正常, 1=故障（越上限/越下限/压差异常任一触发）                         |
| 24   | 目标电压    | R/W  | 22000  | 控母目标值 ×100（22000=220.00V，范围 10000~28000，兼容 110V/220V 系统） |
| 25   | 每档压降    | R/W  | 500    | 每级降压值 ×100（500=5.00V）                                            |
| 26   | 死区上限    | R/W  | 200    | 偏差死区上限 ×100（200=2.00V）                                          |
| 27   | 死区下限    | R/W  | 200    | 偏差死区下限 ×100（200=2.00V）                                          |
| 28   | 控制模式    | R/W  | 0      | 0=自动调压, 1=手动（寄存器12的bit2-4控制Y2/Y3/Y4）                       |
| 29   | 最大压降    | R/W  | 3500   | 硅链最大降压值 ×100（3500=35V→220V系统，110V系统改为2100）             |

## 重要规则

- 修改代码前先理解完整上下文
- 涉及硬件操作时提醒我注意安全
- 修改后检查编译是否通过
- 不要随意删除现有功能代码
- 已用引脚不可复用，新增功能优先使用空闲引脚（PA4-PA7, PA11-PA12 等）
- Y2/Y3/Y4 已分配给硅链控制，Y0 为报警输出，Y1 空闲可用
- **减少任务量，减少代码更改，保证基础项目功能实现**（不新增 FreeRTOS 任务、复用现有任务循环、尽量在现有代码框架内扩展）
