# 直流屏硅链调压器 — 项目交接文档

> **编写日期**：2026-06-23
> **固件版本**：260604
> **作者**：zhifei33
> **面向读者**：小白

---

## 目录

1. [项目概述](#1-项目概述)
2. [硬件架构](#2-硬件架构)
3. [软件架构](#3-软件架构)
4. [完整文件结构](#4-完整文件结构)
5. [开发历程（从0到1）](#5-开发历程从0到1)
6. [环境搭建（零基础版）](#6-环境搭建零基础版)
7. [编译与烧录](#7-编译与烧录)
8. [上位机使用指南](#8-上位机使用指南)
9. [代码导读（零基础版）](#9-代码导读零基础版)
10. [上位机源码导读](#10-上位机源码导读)
11. [常见问题排查](#11-常见问题排查)
12. [维护与升级建议](#12-维护与升级建议)
13. [关键修改记录](#13-关键修改记录)
14. [附录：技术术语表](#14-附录技术术语表)

---

## 1. 项目概述

### 1.1 这是什么？

**直流屏硅链调压器**是一个嵌入式智能控制器，用于直流屏（电力系统中的直流配电柜）中自动调节"控母电压"。

**通俗理解**：电力系统中有两个重要电压：

- **合母电压 (HM)**：电池组输出的总电压，约 220V DC
- **控母电压 (KM)**：给控制设备供电的电压，需要稳定在目标值（如 220V）

由于电池组电压会波动，需要**硅链降压器**来调节。硅链内部有 7 个硅二极管串联，通过 3 个继电器(Y2/Y3/Y4)组合出 8 种连接方式（35V 的降压范围。

**本项目做的三件事**：

1. 📊 **采集**：读取 HM/KM 电压（16位ADC）+ 8路数字输入(X0~X7)
2. 🎮 **控制**：根据 KM 偏差自动切换硅链档位，维持 KM = 目标值
3. 📡 **通信**：通过 Modbus RTU(RS485) 或 Modbus TCP(以太网) 与上位机通信

### 1.2 系统组成

```
┌─────────────────────────┐     RS485/以太网      ┌──────────────────┐
│   STM32 控制板 (下位机)  │ ◄──────────────────► │  PC 上位机监控软件  │
│                         │    Modbus RTU/TCP     │  (Python tkinter) │
│  - 采集HM/KM电压         │                       │                   │
│  - 8路数字输入            │                       │  - 实时数据显示     │
│  - 6路数字输出            │                       │  - 参数读写配置     │
│  - 硅链自动调压           │                       │  - 档位可视化       │
│  - EEPROM掉电保存         │                       │  - 电压一键校准     │
└─────────────────────────┘                       └──────────────────┘
```

### 1.3 关键指标

| 项目          | 数值                              |
| :------------ | :-------------------------------- |
| MCU           | STM32F103C8T6 (Cortex-M3, 72MHz)  |
| Flash 占用    | ~80% (52KB/64KB)                  |
| RAM 占用      | ~18% (3.6KB/20KB)                 |
| 数字输入      | 8路 (X0~X7)，光耦隔离，低电平有效 |
| 数字输出      | 6路 (Y0~Y5)，开漏模式             |
| 模拟输入      | 4路 (ADS1115, 16位ADC, I2C)       |
| 硅链档位      | 8档 (G0~G7)，3继电器编码          |
| 降压范围      | 0~35V (220V系统)                  |
| Modbus 寄存器 | 地址 0~32                         |

---

## 2. 硬件架构

### 2.1 核心芯片

| 芯片          | 用途         | 通信接口         |
| :------------ | :----------- | :--------------- |
| STM32F103C8T6 | 主控 MCU     | —               |
| ADS1115       | 16位4通道ADC | I2C (地址0x48)   |
| MAX485        | RS485收发器  | UART (PB10/PB11) |
| W5500         | 以太网控制器 | SPI              |
| GS8331 (×2)  | 运放跟随器   | —               |

### 2.2 引脚分配速查

```
         STM32F103C8T6
      ┌─────────────────┐
      │                 │
 PB12 ├─ X0 (手自动开关)  │      PA15 ├─ Y0 (报警输出)
 PB13 ├─ X1              │      PB3  ├─ Y1 (通用)
 PB14 ├─ X2              │      PB4  ├─ Y2 (硅链继电器1)
 PB15 ├─ X3              │      PB5  ├─ Y3 (硅链继电器2)
 PA8  ├─ X4              │      PB6  ├─ Y4 (硅链继电器3)
 PB0  ├─ X5              │      PB7  ├─ Y5 (通用)
 PA11 ├─ X6              │
 PA12 ├─ X7              │      PC13 ├─ ERROR_LED
      │                 │      PC14 ├─ RUN_LED
 PC15 ├─ SW_B1 (站号0)    │
 PA0  ├─ SW_B2 (站号1)    │      PB10 ├─ RS485 TX
 PA1  ├─ SW_B3 (站号2)    │      PB11 ├─ RS485 RX
 PA2  ├─ SW_B4 (波特率0)   │      PB1  ├─ RS485 EN
 PA3  ├─ SW_B5 (波特率1)   │
      │                 │      PB8  ├─ I2C SCL
 PB9  ├─ I2C SDA          │      PA9  ├─ 调试TX (115200)
      │                 │      PA10 ├─ 调试RX
      └─────────────────┘
```

> ⚠️ **重要**：PA15/PB3/PB4 默认被 JTAG 占用，代码中通过 `AFIO->MAPR` 禁用了 JTAG-DP 才能当 GPIO 使用。烧录时只用 SWD（PA13/PA14），不能使用 JTAG。

### 2.3 硅链继电器编码表

| 档位 | Y2(PB4) | Y3(PB5) | Y4(PB6) | 降压值(220V) | 降压值(110V) |
| :--: | :-----: | :-----: | :-----: | :----------: | :----------: |
|  G0  |    0    |    0    |    0    |     35V     |     21V     |
|  G1  |    1    |    0    |    0    |     30V     |     18V     |
|  G2  |    0    |    1    |    0    |     25V     |     15V     |
|  G3  |    1    |    1    |    0    |     20V     |     12V     |
|  G4  |    0    |    0    |    1    |     15V     |      9V      |
|  G5  |    1    |    0    |    1    |     10V     |      6V      |
|  G6  |    0    |    1    |    1    |      5V      |      3V      |
|  G7  |    1    |    1    |    1    |   0V(直通)   |      0V      |

> 1=继电器导通(LOW), 0=继电器断开(HIGH)
> 编码与 SCTY-D1 用户手册表3 一致

### 2.4 模拟量信号链路

```
高压直流(HM/KM) → 电阻分压91:1 → EXAI端子 → R64/R66(10kΩ) ─┬─→ LM358跟随 → ADS1115
                                                    R65/R67(22kΩ)
                                                       │
                                                      GND
```

**ADC 转换公式（纯整数运算，避免 Cortex-M3 无 FPU 的性能损失）**：

```
V_bus = (ADC × 4096 × 32 × calibration) / 720874000
```

---

## 3. 软件架构

### 3.1 技术栈一览

| 层         | 技术                 | 版本        |
| :--------- | :------------------- | :---------- |
| 构建系统   | PlatformIO           | VSCode 插件 |
| MCU框架    | Arduino (STM32duino) | 2.12.0      |
| 实时系统   | FreeRTOS             | 10.3.2      |
| 编译器     | arm-none-eabi-gcc    | 12.3.1      |
| 语言       | C++                  | gnu++17     |
| 上位机语言 | Python               | 3.11        |
| 上位机GUI  | tkinter (Python自带) | —          |

### 3.2 FreeRTOS 任务架构

```
优先级 6 (最高):  WatchdogTask    每200ms喂狗      堆栈96W
优先级 5:         X_filter        每1ms消抖滤波    堆栈96W
优先级 4:         ModbusRTUTask   处理RS485请求    堆栈128W
优先级 4:         ModbusTCPTask   处理TCP请求      堆栈256W
优先级 3:         IICTask         每秒读ADS1115    堆栈384W
优先级 3:         MainTask        调压+报警+IO刷新  堆栈256W
```

**设计原则**：安全→采集→通信→业务，优先级递减

### 3.3 程序启动流程

```
上电 → setup()
        ├─ 串口初始化 (PA9/PA10, 115200)
        ├─ Load_Parameter()     ← 从EEPROM加载参数
        ├─ GPIO_Init()           ← 初始化所有IO + 读拨码
        ├─ ModbusRTU_Initialize() ← 初始化Modbus RTU
        ├─ ModbusTCP_Initialize() ← 初始化Modbus TCP
        └─ xTaskCreate(CreateTaskMethods)
             ├─ WatchdogTask
             ├─ X_filter
             ├─ ModbusRTUTask
             ├─ ModbusTCPTask
             ├─ IICTask
             └─ MainTask
             └─ vTaskStartScheduler()  ← 启动调度，永不返回
```

### 3.4 硅链调压算法

```
每1秒执行:
  1. 读取 KM 电压 (寄存器21)
  2. deviation = TargetVoltage - KM
  3. 若 deviation > DeadBand  → 升档 (减小降压)
     升档数 = deviation / dynStepVoltage
  4. 若 -deviation > DeadBand → 降档 (增大降压)
     降档数 = -deviation / dynStepVoltage
  5. 档位钳位至 0~7
  6. SetGearOutput(gear) → 驱动Y2/Y3/Y4
  7. 更新 寄存器22 (当前档位)
```

**动态压差学习**：每次调档后对比调前/调后KM电压，计算实际每档压降，自适应压降。计算值超出 1V~5V 范围时回退默认 3.50V。

**死区作用**：防止 KM 在目标值附近微小波动时继电器频繁切换（类似空调温控的"回差"）。

---

## 4. 完整文件结构

```
GL/                                    ← 项目根目录
├── README.md                          ← 项目说明（面向使用者）
├── CLAUDE.md                          ← AI助手配置（面向AI开发辅助）
├── platformio.ini                     ← PlatformIO 构建配置 ⭐
├── .gitignore                         ← Git 忽略规则
│
├── src/                               ← 固件源码
│   └── main.cpp                       ← 程序入口 setup()/loop()
│
├── include/                           ← 固件头文件
│   ├── myTask.h                       ← 所有FreeRTOS任务 + 硅链控制 ⭐
│   ├── myModbus.h                     ← Modbus RTU/TCP 初始化
│   ├── IO_Setting.h                   ← GPIO引脚定义 + 输入滤波 + ADC换算
│   ├── Parameter_Config.h             ← 参数结构体 + EEPROM读写
│   ├── myADS1115.h                    ← ADS1115 驱动封装
│   └── myShowMsg.h                    ← 串口调试打印
│
├── tools/                             ← 上位机软件 + 调试工具 ⭐
│   ├── 硅链控制板_上位机.py            ← PC监控主程序 (Python tkinter)
│   ├── modbus_monitor.py              ← Modbus串口监听工具
│   ├── modbus_poll.py                 ← Modbus波特率探测+轮询
│   ├── simulate_regulation.py         ← 硅链调压算法模拟验证
│   ├── requirements.txt               ← Python依赖 (pyserial, minimalmodbus)
│   ├── build_exe.bat                  ← 打包为exe的批处理脚本
│   ├── SiliconChainMonitor.spec       ← PyInstaller打包配置
│   ├── dist/
│   │   └── SiliconChainMonitor.exe    ← 独立exe (可拷贝到U盘运行)
│   └── 项目参考文档.md                ← 详细技术参考文档
│
├── Resource/                          ← 编译脚本
│   └── extra_script.py                ← 编译后生成HEX文件
│
├── PLC/                               ← PLC 梯形图参考
├── remote-ioguide/                    ← C#上位机(已废弃)
└── .pio/                              ← PlatformIO编译产物(自动生成)
```

> ⭐ 标记的为最核心文件，接手后优先阅读

---

## 5. 开发历程（从0到1）

按时间顺序，记录了每个版本做了什么、解决了什么问题：

| 日期 | 提交        | 做了什么                                   | 为什么                                         |
| :--- | :---------- | :----------------------------------------- | :--------------------------------------------- |
| 初始 | `95fe8fd` | Modbus-IO 远程IO项目初始版本               | 搭建基础IO采集+Modbus通信框架                  |
| 早期 | `e3375d7` | 修复档位步数计算bug，新增最大压降参数      | 调压步数计算有误，需加参数限制                 |
| 早期 | `310e8ab` | 清理注释、更新引脚文档                     | 代码整理                                       |
| 早期 | `964cec1` | 硅链7档→8档，EEPROM版本校验               | 升级为8档控制（含G0全断），防止EEPROM数据错位  |
| 中期 | `8500ab5` | 自动/手动模式改为X0硬件引脚判断            | 支持外部硬件开关控制手自动切换                 |
| 中期 | `8e945ce` | 清理ControlMode死字段，手动模式报警统一    | 代码清理，解决字段冗余                         |
| 中期 | `f5d7eb9` | 固件版本260604，新增上位机工具集           | 提供完整的PC端监控软件                         |
| 中期 | `48ed7b7` | 修复5个代码缺陷                            | 除以零保护、溢出修复、ADC整数优化等            |
| 中期 | `21cbdfa` | 重写 README                                | 完善项目文档                                   |
| 中期 | `0af896e` | 目录清理，删除废弃文件                     | 项目整理                                       |
| 后期 | `c2a57d6` | 分压系数 99:1→91:1                        | 适配新PCB电阻（R上=900kΩ/R下=10kΩ）          |
| 后期 | `ba131d2` | Modbus校验位可配置（寄存器30）             | 支持8N2/8E1/8O1三种校验模式                    |
| 后期 | `ae5c239` | 代码梳理+双源优先级+参数校验               | 寄存器28>X0的优先级逻辑，PLC兼容               |
| 后期 | `c12131c` | 校准后自动保存EEPROM                       | 修复校准系数不生效问题                         |
| 后期 | `70e562c` | 校准范围对齐+重置默认按钮                  | 上位机校准功能完善                             |
| 后期 | `4a9ce49` | 取消校准系数范围限制                       | 适应各种分压比                                 |
| 后期 | `818324b` | ADC公式加入1.4545运放前端分压系数          | 硬件增加LM358跟随电路                          |
| 后期 | `52982e4` | ADC返回uint32_t防溢出截断                  | 极端校准系数下超65535被uint16截断→恶性循环Bug |
| 后期 | `55b0119` | 精简CLAUDE.md                              | 文档整理                                       |
| 后期 | `2fc9a80` | X0手自动动画+自动模式锁定硅链+动态档位显示 | 上位机体验优化                                 |
| 最后 | `da3809d` | GPIO初始化禁用JTAG                         | 释放PA15/PB3/PB4为GPIO                         |

**开发周期**：约 6 个月，共 20+ 次提交，从基础IO采集逐步演进到完整的硅链调压控制系统。

---

## 6. 环境搭建（零基础版）

### 6.1 需要的软件

| 软件                      | 用途       | 下载                              |
| :------------------------ | :--------- | :-------------------------------- |
| **VSCode**          | 代码编辑器 | https://code.visualstudio.com/    |
| **PlatformIO 扩展** | 编译+烧录  | VSCode内搜索安装                  |
| **Python 3.11+**    | 运行上位机 | https://www.python.org/downloads/ |
| **Git**             | 版本管理   | https://git-scm.com/              |

### 6.2 安装步骤

**Step 1 — 安装 VSCode**

1. 下载 VSCode 安装包
2. 安装时勾选"添加到PATH"
3. 打开 VSCode，点击左侧扩展图标（Ctrl+Shift+X）
4. 搜索 `PlatformIO IDE` → 安装

**Step 2 — 安装 Python**

1. 下载 Python 3.11+ 安装包
2. **重要**：安装时勾选"Add Python to PATH"
3. 验证安装：打开命令行，输入 `py --version`，应显示 Python 3.11.x

**Step 3 — 安装上位机依赖**

```bash
cd GL\tools
py -m pip install -r requirements.txt
```

这会安装 `pyserial`（串口通信）和 `minimalmodbus`（Modbus协议）。

**Step 4 — 打开项目**

1. 打开 VSCode
2. 文件 → 打开文件夹 → 选择 `GL` 目录
3. 等待 PlatformIO 自动加载（右下角显示进度）

---

## 7. 编译与烧录

### 7.1 编译

在 VSCode 中：

- 点击底部状态栏的 **✓** (Build) 图标
- 或在终端运行：`pio run`

编译成功后显示：

```
RAM:   [==        ]  17.9% (used 3656 bytes from 20480 bytes)
Flash: [========  ]  80.2% (used 52576 bytes from 65536 bytes)
========================= [SUCCESS] =========================
```

编译产物在 `.pio/build/genericSTM32F103C8/`：

- `firmware.hex` — HEX烧录文件
- `firmware.bin` — 二进制固件
- `firmware.elf` — ELF调试文件

### 7.2 烧录（ST-Link方式）

1. 用 ST-Link 连接电脑和控制板的 SWD 接口（4根线：3.3V, GND, SWDIO, SWCLK）
2. 在 VSCode 中点击底部状态栏的 **→** (Upload) 图标
3. 等待显示 `[SUCCESS]`

### 7.3 烧录（串口方式）

> 需要先将 BOOT0 跳线置1，按复位，然后烧录，烧录完后 BOOT0 置0

1. 修改 `platformio.ini`：

```ini
upload_port = COM3          # 改为实际串口号
upload_protocol = serial
upload_speed = 115200
```

2. 点击 Upload

---

## 8. 上位机使用指南

### 8.1 直接运行（需要Python环境）

```bash
cd GL\tools
py 硅链控制板_上位机.py
```

### 8.2 独立运行（不需要Python环境）

将 `tools\dist\SiliconChainMonitor.exe` 拷贝到U盘，任何Windows电脑双击即可运行。

### 8.3 重新打包（修改源码后）

```bash
cd GL\tools
build_exe.bat
```

### 8.4 上位机界面说明

```
┌─────────────────────────────────────────────────┐
│  通信设置                                        │
│  串口: [COM3▼] 波特率: [115200▼] 校验位: [奇校验▼] │
│  从站ID: [1]  [连接设备] [断开连接]  ●已连接      │
├────────────────────┬────────────────────────────┤
│  设备信息           │  监测数据                    │
│  从站ID: 1          │  合母电压 HM    控母电压 KM  │
│  波特率: 115200     │    243.00V       220.00V   │
│  控制模式: 跟随X0   │  当前档位  报警    目标电压  │
│  [保存][重载][重启] │    G7/7     正常    220.00V │
│  [恢复出厂]         │  ┌─档位可视化条─┐           │
├────────────────────┤  │ G0↓35V G1↓30V...│        │
│  IO 状态            │  └──────────────┘           │
│  ┌自动┐┌手动┐       │  参数设置                    │
│  │绿 ││灰 │ X0      │  目标电压: [220]V [0~300V]   │
│  └───┘└───┘         │  每档压降: [3.5]V [1~5V]     │
│  X1~X7 状态灯       │  死区上限: [3.5]V [1~5V]     │
│  模拟量 AI0~AI3     │  死区下限: [3.5]V [1~5V]     │
│  数字输出 Y0~Y5     │  最大压降: [35]V [5~50V]     │
│                     │  🔧电压校准 (HM/KM)          │
├────────────────────┴────────────────────────────┤
│  通信日志                                        │
│  [09:30:01] 连接成功! 设备固件版本: 260604        │
│  [09:30:02] 自动轮询已启动 (1秒间隔)              │
└─────────────────────────────────────────────────┘
```

### 8.5 关键操作

| 操作       | 方法                                         | 说明                   |
| :--------- | :------------------------------------------- | :--------------------- |
| 连接设备   | 选串口→点"连接设备"                         | 自动读取所有参数       |
| 修改参数   | 输入框填写→点"写入"                         | 固件自动校验范围       |
| 保存参数   | 点"保存参数"                                 | 写入EEPROM，掉电不丢失 |
| 切换手自动 | 下拉框选模式                                 | 或通过X0硬件开关       |
| 电压校准   | 用万用表测实际电压→填入"实测"→点"一键校准" | 自动计算校准系数并保存 |
| 恢复出厂   | 点"恢复出厂"→确认                           | 所有参数回到默认值     |

---

## 9. 代码导读（零基础版）

### 9.1 固件入口：`src/main.cpp`

```cpp
void setup() {
    Serial.begin(115200);       // 启动调试串口
    Load_Parameter();           // 从EEPROM加载参数
    GPIO_Init();                // 初始化所有引脚
    ModbusRTU_Initialize();     // 初始化Modbus RTU
    ModbusTCP_Initialize();     // 初始化Modbus TCP
    xTaskCreate(...);           // 创建所有FreeRTOS任务
    vTaskStartScheduler();      // 启动任务调度
}
void loop() { /* 空，永远不会执行 */ }
```

> 用 Arduino 框架写 FreeRTOS 的惯例：`setup()` 做初始化，创建任务后 `vTaskStartScheduler()` 接管一切，`loop()` 留空。

### 9.2 引脚配置：`include/IO_Setting.h`

这个文件包含所有硬件相关的定义：

- **引脚定义**（第16~35行）：`#define Temp_X0 PB12` 等
- **GPIO初始化**（第144~202行）：`GPIO_Init()` 函数
- **输入消抖**（第205~228行）：`X_filter` 任务，每1ms运行
- **ADC转换公式**（第126~131行）：纯整数运算避免浮点
- **硅链继电器编码表**（第84~93行）：3继电器→8档位映射

**关键：输入消抖逻辑**

```cpp
// 读取X0引脚
if (digitalRead(Temp_X0) == HIGH)   // 引脚高电平
{
    x_buffer[0] = 0;               // 清空计数
    Input.X0 = 0;                  // 立即确认为"断开"
}
else                                 // 引脚低电平
{
    if (x_buffer[0] < FilterTime)   // 还没到滤波时间
        x_buffer[0]++;             // 递增计数
    else
        Input.X0 = 1;              // 达到滤波时间，确认为"闭合"
}
```

**为什么这样设计？**

- 高→低（闭合）：延迟确认（防抖动）
- 低→高（断开）：立即确认（快速响应）
- 滤波时间可配置：默认5ms，通过Modbus寄存器4修改

### 9.3 参数管理：`include/Parameter_Config.h`

- **参数结构体**（第40~63行）：定义了所有可掉电保存的参数
- **EEPROM读写**（第76~101行）：`Save_Parameter()` / `Load_Parameter()`
- **出厂恢复**（第87~100行）：`Parameter_Init()`

**EEPROM安全机制**：

- `InitFlag`（魔数66）：首次上电或数据损坏时自动恢复出厂
- `ConfigVersion`（版本号）：结构体变更时自动检测不匹配
- 被动保存：只有上位机显式发送保存指令才写EEPROM，避免写穿

### 9.4 核心逻辑：`include/myTask.h`

这是最核心的文件，包含所有 FreeRTOS 任务：

**WatchdogTask**（第31行）

- 每200ms喂狗一次（超时400ms的一半）
- 检测看门狗是否触发过复位

**MainTask**（第253行）

- 每1秒执行一次硅链调压
- 计算 deviation = 目标 - KM
- 超出死区则调档
- 包含动态压差学习算法

**CreateTaskMethods** — 创建所有任务的入口函数

### 9.5 Modbus 通信：`include/myModbus.h`

- 初始化 Modbus RTU 和 TCP
- **关键设计**：两个协议共享同一个寄存器数组 `_regs[33]`
- 寄存器地址 0~32 均可通过 RTU 或 TCP 读写

**库修改注意**：为实现共享寄存器，已将 Modbus-Serial 和 Modbus-Ethernet 库中的 `_regs_head` / `_regs_last` 从 `private` 改为 `public`。更新库版本后需重新修改！

---

## 10. 上位机源码导读

### 10.1 文件：`tools/硅链控制板_上位机.py` (1319行)

**整体结构**：

```python
# ========== 第1~6行：文档字符串 ==========

# ========== 第9~16行：导入 ==========
import tkinter as tk          # GUI框架（Python自带）
import serial                 # 串口通信
import minimalmodbus          # Modbus协议

# ========== 第17~30行：依赖检查 ==========
try:
    import minimalmodbus
except ImportError:
    print("请先安装依赖...")
    sys.exit(1)

# ========== 第33~99行：常量定义 ==========
REG_VERSION = 0               # Modbus寄存器地址
REG_HM_VOLT = 20
# ... 颜色、波特率映射、IO名称等

# ========== 第103行：主程序类 ==========
class SiliconChainMonitor:
    def __init__(self, root):
        # 初始化通信对象、数据缓存、UI
  
    # ---- UI构建 (第166~581行) ----
    def _build_connection_frame()  # 通信设置区
    def _build_device_info()       # 设备信息区
    def _build_io_status()         # IO状态区
    def _build_monitor()           # 监测数据区
    def _build_params()            # 参数设置区
    def _build_log_frame()         # 日志区
  
    # ---- 通信 (第587~780行) ----
    def _connect()                 # 连接设备
    def _disconnect()              # 断开连接
    def _poll_loop()               # 后台轮询（1秒间隔）
  
    # ---- Modbus读写 (第747~843行) ----
    def _safe_read()               # 安全读寄存器
    def _safe_write()              # 安全写寄存器
  
    # ---- UI更新 (第964~1165行) ----
    def _update_io_ui()            # 更新IO状态指示灯
    def _update_monitor_ui()       # 更新监测数据
    def _update_drop_bar()         # 更新档位可视化条
  
    # ---- 电压校准 (第1192~1279行) ----
    def _auto_calibrate()          # 一键校准
    def _reset_calibration()       # 重置默认

# ========== 第1311行：入口 ==========
if __name__ == "__main__":
    main()
```

### 10.2 数据流

```
用户操作 → UI事件 → Modbus读写 → 固件响应
                                ↓
后台轮询(1s) ← 读取寄存器20~25 ← 固件更新数据
     ↓
更新UI显示（电压、档位、IO状态）
```

### 10.3 如果想修改

| 需求             | 改哪里                                       |
| :--------------- | :------------------------------------------- |
| 修改参数默认值   | `_build_params()` 中 params 列表           |
| 修改参数范围提示 | `params` 列表中的 range_hint               |
| 修改窗口大小     | `__init__()` 中的 `geometry("960x780")`  |
| 修改轮询间隔     | `_poll_loop()` 中的 `wait(1.0)`          |
| 修改颜色         | `C_ON`, `C_OFF`, `C_ALARM` 等常量      |
| 添加新参数显示   | 在`params` 列表和 `param_map` 中同步添加 |

---

## 11. 常见问题排查

### 11.1 编译问题

| 问题                     | 原因             | 解决                                         |
| :----------------------- | :--------------- | :------------------------------------------- |
| `PlatformIO not found` | PlatformIO未安装 | VSCode安装PlatformIO扩展                     |
| `library not found`    | 库版本不匹配     | 等待PlatformIO自动下载，或`pio pkg update` |
| Flash溢出                | 代码太大         | 检查是否有调试打印开启                       |

### 11.2 烧录问题

| 问题          | 原因               | 解决                              |
| :------------ | :----------------- | :-------------------------------- |
| ST-Link连不上 | 接线错误或驱动问题 | 检查4根线（3.3V/GND/SWDIO/SWCLK） |
| 串口烧录失败  | BOOT0未置1         | 先置1再上电/复位，烧完置0         |
| 烧录后不运行  | BOOT0仍为1         | BOOT0置0后复位                    |

### 11.3 通信问题

| 问题       | 原因              | 解决                            |
| :--------- | :---------------- | :------------------------------ |
| 设备无响应 | 波特率/站号不匹配 | 检查拨码开关，重启后生效        |
| RS485不通  | A/B接反           | 交换A+/B-                       |
| TCP连不上  | IP不在同一网段    | 检查IP配置（默认192.168.1.168） |

### 11.4 功能问题

| 问题           | 原因                  | 解决                               |
| :------------- | :-------------------- | :--------------------------------- |
| X0指示灯不变化 | PB12接触不良/光耦漏电 | 补焊PB12/加10kΩ上拉电阻           |
| 电压显示不准   | 分压系数不对          | 用万用表校准（上位机一键校准功能） |
| 硅链不调档     | 处于手动模式          | 检查寄存器28和X0开关状态           |
| 调档过于频繁   | 死区太小              | 增大死区参数（建议3.5V）           |
| ADC值为32767   | ADS1115初始化失败     | 检查I2C连接(PB8/PB9)和供电         |
| 看门狗反复复位 | 某任务阻塞超400ms     | 检查ERROR_LED是否闪烁              |

### 11.5 现场调试技巧

1. **开启串口打印**：在 `myShowMsg.h` 中取消注释 `#define UseSerialPrint`，连接 PA9/PA10 (115200bps) 查看运行日志
2. **检查寄存器值**：用上位机直接读取寄存器，比万用表更精准
3. **强制自动模式**：写寄存器28=0，忽略X0开关（方便调试）
4. **恢复出厂**：写寄存器3=66，所有参数回默认

---

## 12. 维护与升级建议

### 12.1 日常维护

| 项目         | 频率       | 操作                         |
| :----------- | :--------- | :--------------------------- |
| 检查端子螺丝 | 每月       | 拧紧所有接线端子             |
| 检查电压校准 | 每季度     | 用万用表对比，必要时重新校准 |
| 检查EEPROM   | 参数异常时 | 恢复出厂后重新配置           |
| 固件备份     | 每次修改后 | 保存`.hex` 文件到安全位置  |

### 12.2 可能的升级方向

| 升级                | 难度 | 说明                                      |
| :------------------ | :--- | :---------------------------------------- |
| 支持110V系统        | 低   | 修改默认参数即可（目标110V，最大压降21V） |
| 增加更多模拟量      | 中   | ADS1115有4通道，AI2/AI3已预留             |
| 扩展IO数量          | 中   | PCF8575驱动已预留（I2C扩展16路IO）        |
| 升级MCU到F4系列     | 高   | 需改引脚映射，但可用硬件FPU               |
| 增加Web配置页面     | 高   | 需在W5500上实现HTTP服务                   |
| PID控制替代步进控制 | 高   | 需更换为连续可调硅链硬件                  |

### 12.3 注意事项

1. **Modbus库修改**：如果更新 `Modbus-Serial` 或 `Modbus-Ethernet` 库版本，需要重新将 `_regs_head` / `_regs_last` 从 `private` 改为 `public`
2. **EEPROM版本号**：如果在 `Parameter_Config` 结构体中增删字段，必须递增 `ConfigVersion`，否则旧数据会错位
3. **空闲引脚**：PA4/PA5/PA6/PA7 空闲可用，新增功能优先使用
4. **JTAG限制**：PA15/PB3/PB4 已通过禁用JTAG释放，烧录只能用SWD（PA13/PA14）

---

## 13. 关键修改记录

> 记录项目最后阶段的重要改动，方便接手者了解当前状态

| 改动             | 文件                                   | 内容                                              |
| :--------------- | :------------------------------------- | :------------------------------------------------ |
| 动态压降范围限制 | `include/myTask.h:390-391`           | `dynStepVoltage` 钳位至 1V~5V，超范围回退 3.50V |
| 上位机范围提示   | `tools/硅链控制板_上位机.py:460-462` | 每档压降/死区范围提示改为 1~5V                    |
| X0光耦上拉       | 硬件                                   | 建议在PB12端增加10kΩ上拉电阻防漏电               |
| 打包为exe        | `tools/dist/SiliconChainMonitor.exe` | 使用PyInstaller打包，可脱离Python环境运行         |
| .gitignore更新   | `.gitignore`                         | 排除PyInstaller构建产物                           |

---

## 14. 附录：技术术语表

| 术语               | 全称                          | 通俗解释                         |
| :----------------- | :---------------------------- | :------------------------------- |
| **MCU**      | Microcontroller Unit          | 单片机，设备的大脑               |
| **GPIO**     | General Purpose I/O           | 通用输入输出引脚                 |
| **ADC**      | Analog to Digital Converter   | 模数转换器，把电压变成数字       |
| **I2C**      | Inter-Integrated Circuit      | 一种2线通信协议（SDA+SCL）       |
| **RS485**    | —                            | 工业现场总线，抗干扰强，可远距离 |
| **Modbus**   | —                            | 工业通信协议，类似"设备的语言"   |
| **RTU**      | Remote Terminal Unit          | Modbus串口模式（二进制）         |
| **TCP**      | Transmission Control Protocol | Modbus网络模式                   |
| **FreeRTOS** | —                            | 嵌入式实时操作系统               |
| **EEPROM**   | Electrically Erasable PROM    | 掉电不丢失的存储器               |
| **PWM**      | Pulse Width Modulation        | 脉冲宽度调制                     |
| **SWD**      | Serial Wire Debug             | 串行调试接口（2线）              |
| **JTAG**     | Joint Test Action Group       | 另一种调试接口（5线，已禁用）    |
| **开漏**     | Open Drain                    | 输出模式：只能拉低不能拉高       |
| **上拉**     | Pull-Up                       | 电阻接到VCC，让引脚默认高电平    |
| **消抖**     | Debounce                      | 过滤开关抖动，防止误判           |
| **死区**     | Dead Band                     | 不调节的区间，防止频繁切换       |
| **滞回**     | Hysteresis                    | 类似空调温控的"回差"             |
| **分压比**   | Voltage Divider Ratio         | 用两个电阻把高电压降到可测量范围 |
| **校准系数** | Calibration Factor            | 修正电阻误差的倍数               |

---

## 附：快速参考卡片

### 常用命令

```bash
# 编译固件
pio run

# 烧录固件
pio run --target upload

# 运行上位机
cd tools && py 硅链控制板_上位机.py

# 打包上位机为exe
cd tools && build_exe.bat

# 查看git历史
git log --oneline

# 安装Python依赖
cd tools && py -m pip install -r requirements.txt
```

### Modbus 寄存器速查

| 地址  | 名称          | 用途                            |
| :---- | :------------ | :------------------------------ |
| 0     | 固件版本      | 只读，如260604                  |
| 3     | 参数操作      | 10=保存 20=重载 30=重启 66=出厂 |
| 11    | 输入状态      | bit0=X0...bit7=X7               |
| 12    | 输出状态      | bit0=Y0...bit5=Y5               |
| 15/16 | AI0/AI1       | ADC原始值                       |
| 20/21 | HM/KM电压     | ×100，如22000=220.00V          |
| 22    | 当前档位      | 0~7                             |
| 23    | 报警状态      | 0=正常 1=故障                   |
| 24    | 目标电压      | ×100，默认22000                |
| 25    | 每档压降      | ×100，默认350(3.50V)           |
| 28    | 控制模式      | 0=强制自动 1=强制手动 2=跟随X0  |
| 31/32 | HM/KM校准系数 | ×100，默认9100                 |

---

*本文档随项目一起移交，建议与代码仓库同步保管。如有疑问，可在 Git 提交记录中查找各改动的详细上下文。*
